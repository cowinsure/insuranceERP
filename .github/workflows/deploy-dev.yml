name: Deploy to Dev Server

on:
  push:
    branches:
      - Development

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Build Docker image
        run: |
          docker build -t insurecow/insurecow_warehouse_repo:frontend-insurecowportal-dev .

      - name: Login to Docker Hub
        run: |
          echo "${{ secrets.DOCKER_PASSWORD }}" | docker login -u "${{ secrets.DOCKER_USERNAME }}" --password-stdin

      - name: Push Docker image
        run: docker push insurecow/insurecow_warehouse_repo:frontend-insurecowportal-dev

      - name: Deploy on dev server
        uses: appleboy/ssh-action@v0.1.6
        with:
          host: ${{ secrets.DEV_SERVER_HOST }}
          username: ${{ secrets.DEV_SERVER_USER }}
          key: ${{ secrets.DEV_SERVER_SSH_KEY }}
          port: 22
          script: |
            cd /var/www/dev_insuranceportal_frontend/farmerDashboard
            docker compose pull
            docker compose up -d
            docker compose ps
